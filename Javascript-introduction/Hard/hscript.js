const add = function (firstNumber, secondNumber) {
    return firstNumber + secondNumber;
};

const subtract = function (firstNumber, num2) {
    return firstNumber - secondNumber;
};

const multiply = function (firstNumber, num2) {
    return (firstNumber * secondNumber);
};

const divide = function (firstNumber, num2) {
    return (firstNumber / secondNumber);
};
